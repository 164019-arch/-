กว้าง = float(input("กรอกความกว้าง: "))
ยาว = float(input("กรอกความยาว: "))

if กว้าง > 0 and ยาว > 0:
    พื้นที่ = กว้าง * ยาว
    print("พื้นที่สี่เหลี่ยมผืนผ้า =", พื้นที่)
else:
    print("กรุณากรอกตัวเลขที่มากกว่า 0")

